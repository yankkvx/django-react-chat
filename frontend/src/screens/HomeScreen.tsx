import { Box, CssBaseline } from "@mui/material";

const HomeScreen: React.FC = () => {
    return (
        <Box sx={{display: 'flex'}}>
            <CssBaseline />
            HomeScren
        </Box>
    );
};

export default HomeScreen;
